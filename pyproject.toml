[project]
name = "holly-prompt"
version = "0.1.0"
description = "ComfyUI custom nodes for prompt orchestration and memory-backed prompt workflows."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
